# =============================================================================
# Week 4 Production Features - Environment Configuration
# =============================================================================

# ===== REQUIRED API KEYS =====
# OpenAI API key (REQUIRED for all functionality)
OPENAI_API_KEY=your_openai_key_here

# Google Custom Search API (Optional - for real web search)
GOOGLE_API_KEY=your_google_key_here
GOOGLE_CSE_ID=your_custom_search_engine_id

# Pinecone Vector Database (Optional - for real document search)
PINECONE_API_KEY=your_pinecone_key_here
PINECONE_ENVIRONMENT=us-east-1-aws


# ===== WEEK 4: PRODUCTION CONFIGURATION =====

# Environment designation (development, staging, production)
ENVIRONMENT=development

# Cost monitoring and budget protection
DAILY_SPENDING_LIMIT=100.00
ALERT_THRESHOLD=70.0
PER_USER_QUOTA=50

# Security and rate limiting
MAX_REQUESTS_PER_MINUTE=10

# Caching configuration
CACHE_TTL_SECONDS=3600


# =============================================================================
# CONFIGURATION NOTES
# =============================================================================
#
# 1. OPENAI_API_KEY is REQUIRED for the application to run
# 2. Google and Pinecone keys are optional - app will use mock data without them
# 3. Week 4 production features include:
#    - Cost monitoring and budget alerts ($100 daily limit default)
#    - Security validation (prompt injection, PII detection, rate limiting)
#    - Error handling with retry logic and circuit breakers
#    - Response caching and model cascading for cost optimization
#    - Production readiness checklist
#    - Comprehensive evaluation framework (10 tests)
#
# 4. To enable real APIs, check "🔴 Use Real APIs" in the sidebar
# 5. Adjust DAILY_SPENDING_LIMIT based on your OpenAI account budget
# 6. ALERT_THRESHOLD is percentage of daily limit that triggers warnings (70%)
#
# =============================================================================
